import * as THREE from 'three'

export const mapBorderBox = new THREE.LineBasicMaterial({ color: 0x0000ff });

export const cluster = new THREE.MeshBasicMaterial({ color: 0xffff00, side: THREE.DoubleSide });
